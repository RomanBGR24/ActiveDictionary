version: "3"

services:
  web:
    build: ./actdict
    command: gunicorn actdict.wsgi:application --bind 0.0.0.0:8000
    expose:
      - 8000

  nginx:
    build: ./nginx
    ports:
      - 80:80
    depends_on:
      - web
